# core.autocrlf=true переписывает рабочую копию в CRLF при каждом checkout.
# Для миграций это ловушка: Prisma хранит SHA-256 файла в _prisma_migrations и
# при несовпадении отказывается деплоить уже применённую миграцию. Держим их LF.
*.sql text eol=lf
